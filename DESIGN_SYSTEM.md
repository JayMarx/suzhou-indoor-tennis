# DESIGN SYSTEM — 苏州室内网球场速查

## Stack（技术栈）
**Vanilla（原生 HTML/CSS/JS，多页静态站）**。理由：零依赖、GitHub Pages 直接托管、手机首屏加载最快、无构建步骤。共享数据放 `data.js`，共享样式放 `styles.css`。

## Mood（氛围）
清爽运动风 —— 干净、有活力、可信赖。像一块刚划好线的室内球场：绿、白、利落。

## Mode（明暗）
浅色为主（白底卡片 + 浅灰背景）。顶栏用网球绿渐变，深色状态栏文字反白。

## Motion（动效）
克制。卡片点击有轻微按压反馈；返回顶部平滑滚动；筛选切换无跳动。不做浮夸动画（手机性能优先）。

## 色彩 Tokens
- `--green` #0b7d3e（主色，按钮/顶栏/强调）
- `--green-d` #095f2f（深绿，渐变/文字）
- `--lime` #c6f24e（网球荧光点缀，仅少量用于高亮/hero）
- `--bg` #f3f5f4（页面背景）
- `--card` #ffffff（卡片）
- `--text` #1c2620（正文）
- `--sub` #65726b（次要文字）
- 语义标签：室内=绿；气膜=蓝(#2456c7/#eaf1ff)；专业=橙(#c26a12/#fff2e6)

## 字体 / 排版
- 系统字体栈（-apple-system / PingFang SC / Microsoft YaHei）。
- 大字优先：页面标题 19–22px，卡片标题 17px，正文 14–15px，触控目标 ≥44px。
- 行高 1.5，中文可读性优先。

## 组件
- **顶栏**：绿色渐变，sticky，含标题 + 搜索框（首页/列表页）。
- **筛选 chips**：横向滚动胶囊，选中填充绿色。
- **场馆卡片**：白底圆角(16px)、轻阴影、标签、地址、距离、备注、导航/拨打双按钮、收藏星。
- **底部导航（多页）**：首页 / 全部场馆 / 关于 三个 tab，固定底部，安全区适配。
- **hero（首页）**：图形化网球元素（圆形球 + 球场线），非仿照片；一句 slogan。

## 布局
- 手机优先，单列；≥600px 时卡片两列网格。
- 安全区：`env(safe-area-inset-*)` 适配刘海/底部。

## 数据诚实
- 未核验电话显示「详见苏体通/大众点评」，不造假号码。
- 全站页脚含非官方免责声明。
